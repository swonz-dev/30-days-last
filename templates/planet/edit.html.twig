{% extends 'base.html.twig' %}

{% block title %}Edit Planet{% endblock %}

{% block body %}
<div class="m-4 p-4 bg-gray-800 rounded-lg">
    <div class="flex justify-between">
        <h1 class="text-xl font-semibold text-white mb-4">Edit Planet</h1>

        <div class="mr-4">
            <a href="{{ path('app_planet_index') }}" class="px-4 py-2 mr-2 border border-transparent text-sm font-medium rounded-md text-white bg-gray-600 hover:bg-gray-700 focus:ring-4 focus:ring-gray-300 focus:outline-none">
                Back to list
            </a>
            <div class="inline-block">
                {% if form.vars.data.id|default %}
                    {{ include('planet/_delete_form.html.twig') }}
                {% endif %}
            </div>
        </div>
    </div>

    {{ include('planet/_form.html.twig') }}
</div>
{% endblock %}
